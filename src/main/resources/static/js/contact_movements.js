'use strict';

function openContactChat(id) {
    console.log('open chat with contact: ' + id);
}

function addToContacts(id) {
    console.log('add to contacts user: ' + id);
}

function blockContact(id) {
    console.log('block user: ' + id);
}